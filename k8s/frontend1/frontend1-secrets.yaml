apiVersion: v1
kind: Secret
metadata:
  name: frontend1-secret
  labels:
    app.kubernetes.io/name: frontend1
    app.kubernetes.io/component: dashboard
type: Opaque
# Injected at deploy time from GitHub Secret FRONTEND1_SECRET_KEY via envsubst.
stringData:
  SECRET_KEY: "${FRONTEND1_SECRET_KEY}"
