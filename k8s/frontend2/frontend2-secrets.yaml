apiVersion: v1
kind: Secret
metadata:
  name: frontend2-secret
  labels:
    app.kubernetes.io/name: frontend2
    app.kubernetes.io/component: public
type: Opaque
# Injected at deploy time from GitHub Secret FRONTEND2_SECRET_KEY via envsubst.
stringData:
  SECRET_KEY: "${FRONTEND2_SECRET_KEY}"
