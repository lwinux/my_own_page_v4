apiVersion: v1
kind: Service
metadata:
  name: frontend2
  labels:
    app.kubernetes.io/name: frontend2
    app.kubernetes.io/component: public
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend2
  ports:
    - name: http
      port: 80
      targetPort: http
